import styled from "styled-components";

export const StyledModal = styled.div`
  z-index: 2;
  position: absolute;
  display: grid;
  height: calc(100% - 90px);
  width: 100%;

  bottom: 0;
  margin: auto 0;
  left: 0;
`;

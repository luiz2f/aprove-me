import styled from "styled-components";

export const StyledModal = styled.div`
  z-index: 2;
  position: absolute;
  display: grid;
  width: 100%;
  height: 100%;
  backdrop-filter: blur(2px);
  top: 0;
  margin: auto 0;
  left: 0;
`;

{
  "devDependencies": {
    "turbo": "^1.13.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "test": "turbo run test",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "passport-jwt": "^4.0.1"
  }
}
